name: cursor-beforeShellExecution
input:
  command: string
  cwd: string
output:
  continue: boolean
  permission: [allow, deny, ask]
  user_message: string
  agent_message: string
notes:
  - Deny should set continue=false
